# Source code for pFUnit tests. Note that the module inside containing the tests should have the *same* name
# as the file,  i.e. the module inside 'testExample.pf' should be named 'testExample'.
set (TEST_SOURCES
    )

# Extra Fortran sources used for testing. Could be utility functions, dummy objects, stubs etc.
set (EXTRA_SOURCES
    )

# Extra C/C++ sources used for testing. Could be utility functions, dummy objects, stubs etc.
set (EXTRA_SOURCES_C
    )