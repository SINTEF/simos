############################################
# Definition of library sources
############################################

# Public Fortran sources. Only file names should be listed.
# The path to the folder (`source`) will be prepended all files
set (PUBLIC_SOURCES_FORTRAN
    )

# Internal Fortran sources. Only file names should be listed.
# The path to the folder (`source/internal/`) will be prepended all files
set (INTERNAL_SOURCES_FORTRAN
    )

# Public Fortran sources. Only file names should be listed.
# The path to the folder (`source/`) will be prepended all files
set (PUBLIC_SOURCES_C
    )

# Internal C/C++ sources. Only file names should be listed.
# The path to the folder (`source/internal/`) will be prepended all files
set (INTERNAL_SOURCES_C
    )
